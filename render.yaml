services:
  - type: web
    name: cutoff-analysis-service
    runtime: docker
    plan: free # Optimized for zero-cost deployment
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: college_compass_db
          property: connectionString
      - key: PORT
        value: 8000
      - key: PYTHONPATH
        value: "/app"
    healthCheckPath: /health
    autoDeploy: true
